# 🎯 تعليمات نشر نظام التمويل على Hostinger

## ✅ تم إنشاء حزمة النشر بنجاح!

---

## 📦 الملف الجاهز للرفع

```
📁 tamweel-hostinger-v1.0.tar.gz (14 KB)
```

**الموقع:** `/home/user/webapp/tamweel-hostinger-v1.0.tar.gz`

---

## 🚀 خطوات التنصيب (3 طرق)

### ⭐ الطريقة 1: عبر File Manager (الأسهل - موصى بها)

#### الخطوة 1: تحميل الحزمة

1. حمّل ملف `tamweel-hostinger-v1.0.tar.gz` إلى جهازك
2. سجل دخول إلى **Hostinger hPanel**
3. اذهب إلى **Files → File Manager**
4. افتح مجلد `public_html`

#### الخطوة 2: رفع وفك الضغط

1. اضغط زر **Upload Files**
2. ارفع ملف `tamweel-hostinger-v1.0.tar.gz`
3. بعد انتهاء الرفع:
   - اضغط بزر الماوس الأيمن على الملف
   - اختر **Extract**
   - انتظر حتى ينتهي فك الضغط

#### الخطوة 3: نقل الملفات

1. افتح مجلد `hostinger-deploy`
2. حدد جميع الملفات داخله (Ctrl+A)
3. اضغط **Move**
4. اختر `public_html` كوجهة
5. احذف المجلد الفارغ `hostinger-deploy`

#### الخطوة 4: إعداد قاعدة البيانات

1. في hPanel، اذهب إلى **Databases → MySQL Databases**
2. أنشئ قاعدة بيانات جديدة:
   - Database Name: `tamweel`
   - Username: (سيُنشأ تلقائياً)
   - Password: **أنشئ كلمة مرور قوية**
3. احفظ المعلومات:
   ```
   DB_HOST=localhost
   DB_NAME=u123456_tamweel
   DB_USER=u123456_tamweel
   DB_PASSWORD=your_strong_password
   ```

#### الخطوة 5: تعديل ملف .env

1. في File Manager، افتح مجلد `public_html`
2. انسخ `.env.example` إلى `.env`
3. افتح `.env` للتعديل
4. أدخل معلومات قاعدة البيانات التي حفظتها
5. احفظ الملف

#### الخطوة 6: تنصيب عبر SSH

الآن يجب استخدام SSH لإكمال التنصيب:

```bash
# الاتصال بـ SSH
ssh u123456@yourdomain.com -p 65002

# الانتقال للمجلد
cd ~/public_html

# تثبيت الحزم
npm install

# إعداد قاعدة البيانات
npm run setup-db

# تثبيت PM2
npm install -g pm2

# تشغيل التطبيق
pm2 start server.js --name "tamweel-app"
pm2 save
pm2 startup
```

#### الخطوة 7: تفعيل SSL

1. في hPanel → **Security → SSL**
2. اختر دومينك
3. Install Free SSL (Let's Encrypt)
4. انتظر 5-10 دقائق

#### الخطوة 8: اختبار

افتح: `https://yourdomain.com`

---

### 🔧 الطريقة 2: عبر SSH الكامل (للمحترفين)

```bash
# 1. الاتصال
ssh u123456@yourdomain.com -p 65002

# 2. الانتقال للمجلد
cd ~/public_html

# 3. رفع الملف (استخدم scp من جهازك)
# من جهازك:
scp -P 65002 tamweel-hostinger-v1.0.tar.gz u123456@yourdomain.com:~/public_html/

# 4. فك الضغط
tar -xzf tamweel-hostinger-v1.0.tar.gz
mv hostinger-deploy/* .
rm -rf hostinger-deploy tamweel-hostinger-v1.0.tar.gz

# 5. إعداد البيئة
cp .env.example .env
nano .env  # أدخل معلومات قاعدة البيانات

# 6. التثبيت
npm install
npm run setup-db

# 7. التشغيل
pm2 start server.js --name "tamweel-app"
pm2 save
pm2 startup
```

---

### 🌐 الطريقة 3: عبر FTP (FileZilla)

#### 1. الاتصال بـ FTP

- **Host:** ftp.yourdomain.com
- **Username:** u123456
- **Password:** كلمة مرور FTP
- **Port:** 21

#### 2. رفع الملفات

1. فك ضغط `tamweel-hostinger-v1.0.tar.gz` على جهازك
2. ارفع محتويات مجلد `hostinger-deploy` إلى `public_html`

#### 3. أكمل الخطوات عبر SSH

اتبع الخطوات 4-8 من الطريقة 1

---

## 📋 ما بعد التنصيب

### ✅ قائمة التحقق

- [ ] قاعدة البيانات تعمل
- [ ] الملفات مرفوعة
- [ ] ملف .env معدّل
- [ ] npm install نجح
- [ ] setup-db نجح
- [ ] PM2 يعمل
- [ ] SSL مفعّل
- [ ] الموقع يفتح

### 🔐 أمان مهم

**غيّر كلمات المرور فوراً:**

1. سجل دخول كـ: `superadmin` / `SuperAdmin@2025`
2. اذهب لإدارة المستخدمين
3. غيّر جميع كلمات المرور

### 🧪 اختبار الوظائف

1. **تسجيل الدخول:** ✅
2. **إضافة بنك:** ✅
3. **إضافة نسبة:** ✅
4. **إضافة عميل:** ✅
5. **إنشاء طلب تمويل:** ✅
6. **عرض التقارير:** ✅

---

## 📚 الوثائق المتوفرة

داخل مجلد `public_html`:

1. **QUICK_START_AR.md** - دليل سريع بالعربي (10 خطوات)
2. **HOSTINGER_DEPLOYMENT_GUIDE.md** - دليل شامل مفصل
3. **README.md** - نظرة عامة

---

## 🔧 أوامر مفيدة

### إدارة التطبيق

```bash
pm2 status                    # الحالة
pm2 logs tamweel-app         # السجلات
pm2 restart tamweel-app      # إعادة التشغيل
pm2 stop tamweel-app         # إيقاف
pm2 delete tamweel-app       # حذف
pm2 monit                    # مراقبة مباشرة
```

### نسخ احتياطي

```bash
# قاعدة البيانات
mysqldump -u u123456_tamweel -p u123456_tamweel > backup_$(date +%Y%m%d).sql

# الملفات
tar -czf backup_$(date +%Y%m%d).tar.gz public_html/
```

---

## ❓ حل المشاكل الشائعة

### 1. الموقع لا يفتح

```bash
pm2 status
pm2 logs tamweel-app --lines 50
pm2 restart tamweel-app
```

### 2. خطأ 502 Bad Gateway

```bash
# تحقق من المنفذ
netstat -tuln | grep 3000

# أعد تشغيل Apache
sudo service apache2 restart
```

### 3. خطأ في قاعدة البيانات

```bash
# اختبر الاتصال
mysql -u u123456_tamweel -p u123456_tamweel

# تحقق من .env
cat .env
```

### 4. npm install فشل

```bash
# امسح وأعد المحاولة
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
```

---

## 📞 الدعم

### المستندات

- 📘 QUICK_START_AR.md - للبداية السريعة
- 📕 HOSTINGER_DEPLOYMENT_GUIDE.md - الدليل الكامل
- 📗 README.md - نظرة عامة

### الاتصال

- **دعم Hostinger:** https://www.hostinger.com/contact
- **الوثائق:** https://support.hostinger.com

---

## 🎯 الخلاصة

✅ **الملفات جاهزة**  
✅ **الوثائق كاملة**  
✅ **التعليمات واضحة**

**الآن:** حمّل الملف وابدأ التنصيب! 🚀

---

## 📊 معلومات الحزمة

- **الاسم:** tamweel-hostinger-v1.0.tar.gz
- **الحجم:** 14 KB
- **الإصدار:** 1.0.0
- **التاريخ:** ديسمبر 2024
- **المطور:** GenSpark AI Developer

---

## 🌟 الميزات

✅ خادم Express.js كامل  
✅ قاعدة بيانات MySQL  
✅ نظام مستخدمين متعدد  
✅ إدارة البنوك والنسب  
✅ إدارة العملاء  
✅ طلبات التمويل  
✅ التقارير المتقدمة  
✅ واجهة عربية كاملة  
✅ تصميم متجاوب  
✅ أمان عالي  

---

**🎉 بالتوفيق في التنصيب!**

إذا احتجت مساعدة، راجع الأدلة المرفقة أو تواصل مع الدعم.
