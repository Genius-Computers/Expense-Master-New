# 🚀 دليل التثبيت على Hostinger - خطوة بخطوة

## 📋 **المتطلبات:**

✅ حساب Hostinger نشط  
✅ دومين: `tamweel-calc.com`  
✅ قاعدة بيانات MySQL جاهزة  
✅ صلاحيات SSH  

---

## 🎯 **خطوات التثبيت السريعة:**

### **1️⃣ تحميل الملفات**

قم بتحميل ملف: `tamweel-hostinger-v2.0-mobile.tar.gz`

من هنا: https://github.com/basealsyed2015-source/Expense-Master/tree/genspark_ai_developer

---

### **2️⃣ رفع الملفات إلى Hostinger**

#### **الطريقة الأولى: File Manager (سهلة)**

1. اذهب إلى **Hostinger Panel**
2. اختر **الملفات** (Files)
3. افتح **مدير الملفات** (File Manager)
4. اذهب إلى مجلد `public_html`
5. اضغط **رفع** (Upload)
6. ارفع ملف `tamweel-hostinger-v2.0-mobile.tar.gz`
7. بعد الرفع، اضغط على الملف → **Extract** (فك الضغط)
8. افتح مجلد `hostinger-deploy` الجديد
9. **حدد جميع الملفات** بداخله
10. اضغط **نقل** (Move) → انقلها إلى `public_html`

#### **الطريقة الثانية: FTP (متقدمة)**

```bash
استخدم FileZilla أو أي FTP client
Host: ftp.tamweel-calc.com
Username: u928834852_basealsyed2015
Port: 21
```

---

### **3️⃣ إنشاء قاعدة البيانات**

1. من **Hostinger Panel** → اذهب إلى **Databases**
2. أنشئ قاعدة بيانات جديدة:
   - **اسم القاعدة:** `u928834852_tamweel`
   - **اسم المستخدم:** `u928834852_basealsyed2015`
   - **كلمة المرور:** `Aa@1@2@3@4@5@6` (أو اختر كلمة مرور قوية)
3. **احفظ المعلومات!** ستحتاجها لاحقاً

---

### **4️⃣ تعديل ملف .env**

1. في **File Manager** → افتح مجلد `public_html`
2. ابحث عن ملف `.env.example`
3. **انسخه** وسمّه `.env`
4. **عدّل** ملف `.env` الجديد:

```env
PORT=3000
NODE_ENV=production

# معلومات قاعدة البيانات (من الخطوة 3)
DB_HOST=localhost
DB_USER=u928834852_basealsyed2015
DB_PASSWORD=Aa@1@2@3@4@5@6
DB_NAME=u928834852_tamweel

# مفتاح الجلسة (اتركه كما هو)
SESSION_SECRET=TamweelApp2024SecureKey@!#$%^&*()

# رابط الموقع
APP_NAME=Tamweel Finance System
APP_URL=https://tamweel-calc.com
```

5. **احفظ** الملف

---

### **5️⃣ تفعيل SSH والاتصال**

#### **تفعيل SSH:**
1. من **Hostinger Panel** → اذهب إلى **Advanced**
2. اختر **SSH Access**
3. اضغط **Enable SSH**
4. احفظ معلومات الاتصال:
   ```
   Host: tamweel-calc.com
   Port: 65002
   Username: u928834852
   Password: (نفس كلمة مرور Hostinger أو أنشئ كلمة مرور SSH)
   ```

#### **الاتصال بـ SSH:**

**من Windows:**
```bash
# استخدم PuTTY أو Windows Terminal
ssh u928834852@tamweel-calc.com -p 65002
```

**من Mac/Linux:**
```bash
ssh u928834852@tamweel-calc.com -p 65002
```

**من Android:**
- نزّل تطبيق **Termux** أو **JuiceSSH**
- أدخل نفس الأوامر

---

### **6️⃣ تثبيت وتشغيل التطبيق (عبر SSH)**

بعد الاتصال بـ SSH، نفذ هذه الأوامر **بالترتيب**:

```bash
# 1. الانتقال لمجلد الموقع
cd ~/public_html

# 2. التأكد من وجود الملفات
ls -la

# 3. تثبيت npm packages
npm install

# 4. إنشاء جداول قاعدة البيانات
npm run setup-db

# 5. تثبيت PM2 (مدير التطبيقات)
npm install -g pm2

# 6. تشغيل التطبيق
pm2 start server.js --name "tamweel-app"

# 7. حفظ إعدادات PM2
pm2 save

# 8. تشغيل PM2 تلقائياً عند إعادة التشغيل
pm2 startup
```

---

### **7️⃣ تفعيل SSL (HTTPS)**

1. من **Hostinger Panel** → اذهب إلى **Security**
2. اختر **SSL**
3. اختر الدومين: `tamweel-calc.com`
4. اضغط **Install Free SSL (Let's Encrypt)**
5. انتظر 1-2 دقيقة حتى يتم التفعيل

---

### **8️⃣ اختبار الموقع**

افتح المتصفح واذهب إلى:
```
https://tamweel-calc.com
```

**صفحة تسجيل الدخول:**
```
https://tamweel-calc.com/login.html
```

**حسابات التجربة:**
- المستخدم: `superadmin` / كلمة المرور: `SuperAdmin@2025`
- المستخدم: `admin` / كلمة المرور: `Admin@2025`
- المستخدم: `admin1` / كلمة المرور: `Admin1@2025`

---

## 🔧 **إدارة التطبيق (أوامر PM2)**

```bash
# عرض حالة التطبيق
pm2 status

# إعادة تشغيل التطبيق
pm2 restart tamweel-app

# إيقاف التطبيق
pm2 stop tamweel-app

# عرض السجلات (logs)
pm2 logs tamweel-app --lines 50

# حذف التطبيق من PM2
pm2 delete tamweel-app
```

---

## ❓ **حل المشاكل الشائعة**

### **المشكلة: "Cannot find module..."**
```bash
cd ~/public_html
npm install
pm2 restart tamweel-app
```

### **المشكلة: "Database connection error"**
- تأكد من معلومات قاعدة البيانات في `.env`
- تأكد من إنشاء قاعدة البيانات في Hostinger
- جرب الاتصال يدوياً من SSH

### **المشكلة: "Port already in use"**
```bash
pm2 delete tamweel-app
pm2 start server.js --name "tamweel-app"
```

### **المشكلة: الموقع لا يعمل**
1. تأكد من تشغيل PM2: `pm2 status`
2. راجع السجلات: `pm2 logs tamweel-app`
3. أعد تشغيل التطبيق: `pm2 restart tamweel-app`

---

## 📱 **المميزات المضافة حديثاً:**

✅ تجاوب كامل مع الجوال  
✅ شريط تمرير واضح على الجوال  
✅ أزرار تمرير يمين ويسار للجداول  
✅ تحسينات في حجم الخطوط والأزرار  
✅ تصميم محسّن للمس  

---

## 🆘 **الدعم الفني:**

**Hostinger Support:**
- Live Chat: https://www.hostinger.com/
- البريد: support@hostinger.com
- الهاتف: متوفر في لوحة التحكم

**وثائق Hostinger:**
- https://support.hostinger.com/

---

## ✅ **قائمة التحقق النهائية:**

- [ ] رفعت الملفات إلى `public_html`
- [ ] أنشأت قاعدة بيانات MySQL
- [ ] عدّلت ملف `.env` بالمعلومات الصحيحة
- [ ] اتصلت بـ SSH
- [ ] نفذت `npm install`
- [ ] نفذت `npm run setup-db`
- [ ] شغّلت التطبيق بـ PM2
- [ ] فعّلت SSL
- [ ] اختبرت الموقع - يعمل! ✓

---

🎉 **مبروك! موقعك الآن جاهز على الإنترنت!**

🌐 **الموقع:** https://tamweel-calc.com
